rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permissive rules for MUSA TRADERS - eased access as requested
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
